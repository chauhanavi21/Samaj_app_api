# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# JWT Secret (generate using: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))")
JWT_SECRET=your-secret-key-here

# Server Configuration
PORT=3001
NODE_ENV=development

# Admin Configuration
ADMIN_EMAILS=admin@example.com,another@example.com

# API URL for cron self-ping (production only)
# Local: http://localhost:3001/api/health
# Production (Render): https://your-app.onrender.com/api/health
API_URL=http://localhost:3001/api/health
